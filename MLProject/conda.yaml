name: mlflow-ci
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12.7
  - pip
  - pip:
    - pandas
    - numpy
    - scikit-learn
    - mlflow
    - joblib
    - seaborn
    - matplotlib